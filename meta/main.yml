---
galaxy_info:
  standalone: true
  author: xnoto
  description: >-
    Deploys OpenShift Local (CRC) with custom DNS and TLS.
    Optionally installs ArgoCD GitOps operator and creates an Application.
    Supports SOPS/AGE secret decryption if keys are provided.
  role_name: crc
  company: Make IT Work Cloud
  license: GPL-3.0-or-later
  min_ansible_version: "2.1"
  galaxy_tags:
    - crc
    - openshift
    - argocd
    - gitops
